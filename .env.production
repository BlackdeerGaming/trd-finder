# URL pública donde quede corriendo el backend (server.js) en producción.
# Ejemplos según dónde termine viviendo el backend:
#   https://api.blackdeerbrand.com          (subdominio dedicado para la API)
#   https://blackdeerbrand.com/TRDFinder-api (si el hosting soporta Node en una subruta)
# IMPORTANTE: sin barra final, y debe incluir protocolo (https://).
VITE_API_URL=https://trdfinder-api.onrender.com
